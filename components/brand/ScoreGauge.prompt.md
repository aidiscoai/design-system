The signature AI Discoverability Score gauge — a violet→cyan conic ring with a big animated monospace number. Use it as the focal point of any audit or dashboard.

```jsx
<ScoreGauge value={72} label="AI Discoverability" />
```

Animates from 0 to `value` on mount. Keep one per view — it's the hero metric.
